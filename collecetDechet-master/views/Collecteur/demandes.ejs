<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Tableau de bord - Collecteur</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f5f6f8; font-family: 'Segoe UI', sans-serif; }
    header { background-color: #0d6efd; color: white; padding: 20px; border-radius: 8px; margin-bottom: 30px; }
    table { background: white; border-radius: 8px; overflow: hidden; box-shadow: 0 4px 8px rgba(0,0,0,0.1); }
  </style>
</head>
<body class="container py-4">
<header class="text-center">
  <h1>Bienvenue, <%= collecteur.zone %> — <%= collecteur.nom %> 👷‍♂️</h1>
  <p class="lead">Voici les demandes de collecte à traiter</p>
</header>


<div class="mb-3 text-end">
  <a href="/collecteur/<%= collecteur.id || 1 %>/historique" class="btn btn-outline-primary btn-sm">
    Voir l’historique
  </a>
</div>

<table class="table table-hover align-middle">
  <thead class="table-primary">
    <tr>
      <th>Citoyen</th>
      <th>Type de déchet</th>
      <th>Quantité (kg)</th>
      <th>Date de rencontre</th>
      <th>Statut</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% demandes.forEach(demande => { %>
      <tr>
        <td><%= demande.citoyenId.nom %> (<%= demande.citoyenId.adresse %>)</td>
        <td><%= demande.type %></td>
        <td><%= demande.quantite %></td>
        <td><%= demande.dateRencontre %></td>
        <td><span class="badge bg-warning text-dark"><%= demande.statut %></span></td>
        <td>
          <!-- ✅ Corrigé : bonne route POST -->
          <form method="POST" action="/collecteur/<%= demande._id %>/accepter" style="display:inline;">
            <button class="btn btn-success btn-sm">Accepter</button>
          </form>

          <button class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#rapportModal"
            data-id="<%= demande._id %>">
            Rapporter
          </button>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>

<!-- ✅ Modal de rapport -->
<div class="modal fade" id="rapportModal" tabindex="-1" aria-labelledby="rapportModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header bg-danger text-white">
        <h5 class="modal-title" id="rapportModalLabel">Rapporter une collecte</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
      </div>
      <form method="POST" id="rapportForm">
        <div class="modal-body">
          <input type="hidden" name="collecteId" id="collecteId">
          <div class="mb-3">
            <label for="description" class="form-label">Description du problème :</label>
            <textarea id="description" name="description" class="form-control" required></textarea>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
          <button type="submit" class="btn btn-danger">Envoyer le rapport</button>
        </div>
      </form>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script>
  const rapportModal = document.getElementById('rapportModal');
  rapportModal.addEventListener('show.bs.modal', event => {
    const button = event.relatedTarget;
    const collecteId = button.getAttribute('data-id');
    document.getElementById('collecteId').value = collecteId;
    // ✅ Correction ici : route = /collecteur/:id/rapporter
    document.getElementById('rapportForm').action = `/collecteur/${collecteId}/rapporter`;
  });
</script>

</body>
</html>
