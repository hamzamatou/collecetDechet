<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Historique des collectes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f8f9fa; font-family: 'Segoe UI', sans-serif; }
    header { background-color: #0d6efd; color: white; padding: 20px; border-radius: 8px; margin-bottom: 30px; }
    table { background: white; border-radius: 8px; box-shadow: 0 4px 8px rgba(0,0,0,0.1); }
  </style>
</head>
<body class="container py-4">

<header class="text-center">
  <h1>Historique des collectes 🚛</h1>
  <p class="lead">Toutes les collectes passées ou rapportées</p>
</header>

<div class="mb-3 text-end">
  <a href="/collecteur/demandes" class="btn btn-outline-secondary btn-sm">
    ⬅ Retour au tableau de bord
  </a>
</div>

<table class="table table-striped align-middle">
  <thead class="table-primary">
    <tr>
      <th>Citoyen</th>
      <th>Type</th>
      <th>Quantité</th>
      <th>Date de collecte</th>
      <th>Statut</th>
    </tr>
  </thead>
  <tbody>
    <% historique.forEach(item => { %>
      <tr>
        <td><%= item.citoyen.nom %> (<%= item.citoyen.adresse %>)</td>
        <td><%= item.typeDechet %></td>
        <td><%= item.quantite %> kg</td>
        <td><%= item.dateCollecte %></td>
        <td>
          <% if (item.statut === 'terminée') { %>
            <span class="badge bg-success">Terminée</span>
          <% } else { %>
            <span class="badge bg-danger">Rapportée</span>
          <% } %>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>

</body>
</html>
