<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mes Déchets</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { 
            padding: 20px; 
            background-color: #f8f9fa; 
        }
        header { 
            margin-bottom: 20px; 
        }
        h1 { 
            margin-bottom: 10px; 
        }
        a.back-link {
            text-decoration: none; 
            color: #0d6efd;
        }
        a.back-link:hover {
            text-decoration: underline;
        }
        table th {
            background-color: #0d6efd;
            color: white;
        }
    </style>
</head>
<body>

<header class="d-flex justify-content-between align-items-center">
    <h1>Mes déchets déposés</h1>
    <a class="back-link" href="/citoyen/<%= citoyen.id %>/dashboard">&larr; Retour au dashboard</a>
</header>

<section class="table-responsive">
    <% if (citoyen.dechets && citoyen.dechets.length > 0) { %>
        <table class="table table-striped table-bordered align-middle">
            <thead>
                <tr>
                    <th>Type</th>
                    <th>Quantité (kg)</th>
                    <th>Date dépôt</th>
                    <th>État</th>
                </tr>
            </thead>
            <tbody>
                <% citoyen.dechets.forEach(dechet => { %>
                    <tr>
                        <td><%= dechet.type %></td>
                        <td><%= dechet.quantite %></td>
                        <td><%= new Date(dechet.dateDepot).toLocaleDateString() %></td>
                        <td><%= dechet.etat.replace('_', ' ') %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } else { %>
        <p class="text-muted">Vous n'avez encore déposé aucun déchet.</p>
    <% } %>
</section>

<!-- Bootstrap JS (optionnel si tu veux les composants interactifs) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
