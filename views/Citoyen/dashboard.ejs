<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Dashboard Citoyen</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f1f3f6;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        header {
            margin-bottom: 20px;
            background-color: #0d6efd;
            color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
        nav {
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        nav a {
            text-decoration: none;
            color: #0d6efd;
            font-weight: 500;
        }
        nav a:hover { text-decoration: underline; }

        .form-section {
            margin-bottom: 30px;
        }
        .card {
            margin-bottom: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        table th {
            background-color: #0d6efd;
            color: white;
        }
        input[type="text"], input[type="number"] {
            width: auto;
        }
        .badge {
            font-size: 0.9rem;
        }
    </style>
</head>
<body class="container py-4">

<header class="text-center">
    <h1>Bienvenue, <%= citoyen.nom %> !</h1>
    <p class="lead">Votre espace personnel pour gérer vos dépôts et points</p>
</header>

<nav class="mb-4">
    <span class="fw-bold">Points : <%= citoyen.points %></span>
    <a href="/citoyen/<%= citoyen.id %>/dechets" class="btn btn-outline-primary btn-sm">Voir mes déchets</a>
</nav>

<section class="form-section">
    <div class="card p-4 bg-white">
        <h2 class="mb-3">Déposer un déchet</h2>
        <form action="/api/citoyen/depot" method="POST" class="row g-3 align-items-center">
            <input type="hidden" name="citoyenId" value="<%= citoyen.id %>">
            <div class="col-auto">
                <label for="type" class="form-label">Type de déchet :</label>
                <input type="text" id="type" name="type" required class="form-control">
            </div>
            <div class="col-auto">
                <label for="quantite" class="form-label">Quantité (kg) :</label>
                <input type="number" id="quantite" name="quantite" step="0.1" required class="form-control">
            </div>
            <div class="col-auto align-self-end">
                <input type="submit" value="Déposer" class="btn btn-primary">
            </div>
        </form>
    </div>
</section>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
